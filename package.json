{
  "name": "dynamodb-tools",
  "version": "0.0.8",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "deploy": "npm run build && npm version patch && npm publish",
    "tsc-build": "tsc -d"
  },
  "author": "Ben Gardner <ben@focuswish.com> (http://www.automationfuel.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/cuid": "^1.3.0",
    "@types/node": "^8.5.2",
    "aws-sdk": "^2.176.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "default-key": "0.0.4",
    "jsonwebtoken": "^8.1.0"
  }
}
